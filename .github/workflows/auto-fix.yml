name: Auto-fix CI failures

# Disabled — requires a direct Anthropic API key which claude-code-action
# does not support proxying through third-party providers.
# Auto-fix is handled by a scheduled remote agent instead:
# https://claude.ai/code/routines/trig_01KypEnupAHdSj8xtFJ1WHCZ
on:
  workflow_dispatch:

jobs:
  auto-fix:
    if: false
    runs-on: ubuntu-latest
    steps:
      - run: echo "disabled"
